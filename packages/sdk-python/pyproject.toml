[project]
name = "cpaas"
version = "1.0.0"
description = "Official CPaaS Python SDK"
requires-python = ">=3.10"
dependencies = ["requests>=2.32.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["cpaas*"]
